pymupdf>=1.23
pillow>=10
scikit-learn>=1.3
numpy>=1.24
# Optional OCR Python wrapper. Requires the system tesseract binary too.
# pytesseract>=0.3
# AWS integration (optional — only needed when DUPE_SQS_QUEUE_URL / DUPE_S3_BUCKET / DUPE_DYNAMO_TABLE are set)
boto3>=1.34
